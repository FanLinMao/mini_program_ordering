server:
  port: 18080

spring:
  application:
    name: private-kitchen-admin-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:8020/private_kitchen?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: Flemming@123

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id
      logic-delete-field: del_flag
      logic-delete-value: 1
      logic-not-delete-value: 0

miniapp:
  app-id: wx2b64aa5e34fe7252
  app-secret: f0d85b07082e84559dd0987dd4ab7cc1
  code2session-url: https://api.weixin.qq.com/sns/jscode2session

app:
  cors:
    allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost,http://localhost:*,http://127.0.0.1,http://127.0.0.1:*,http://*,http://*:*,https://*,https://*:*}
